{
  "name": "diigolet2",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Small Diigo highlighter bookmarklet (under Android Chrome's 5,000-character limit) that survives strict Content Security Policies; a helper page on our origin holds the Diigo logic.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.mjs",
    "serve": "node serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
